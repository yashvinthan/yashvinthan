#include <iostream>
using namespace std;

int main() {
    int A[4][4], B[4][4];

    for (int i = 0; i < 4; i++)
        for (int j = 0; j < 4; j++)
            cin >> A[i][j];

    for (int i = 0; i < 4; i++)
        for (int j = 0; j < 4; j++)
            cin >> B[i][j];

    for (int i = 0; i < 4; i++) {
        int left = A[i][0] * B[0][0] + A[i][1] * B[1][0];
        int right = A[i][2] * B[2][2] + A[i][3] * B[3][2];

        cout << left << " " << left << " " << right << " " << right << " " << endl;
    }

    return 0;
}
